import Dropzone from "@/components/dropzone";
import Header from "@/components/header";
import { main } from "ts-node/dist/bin";

export default function Property_page() {
    return (
        <main>
            <Header />
            <Dropzone />
        </main>
    );
}